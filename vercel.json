{
  "version": 2,
  "buildCommand": "npm run build",
  "builds": [
    {
      "src": "server/node-build.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["dist/spa/**"]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server/node-build.ts"
    }
  ]
}
